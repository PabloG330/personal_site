import { defineConfig } from 'astro/config';

// https://astro.build
export default defineConfig({
  site: 'https://pablocardone.com',
  // When you buy a domain, update `site` above. For Cloudflare Pages / Netlify
  // the default static output is exactly what we want — no adapter needed.
});
